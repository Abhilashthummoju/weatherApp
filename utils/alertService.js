const sendAlert = (city, condition) => {
    console.log(`ALERT for ${city}: ${condition}`);
    // Additional functionality for email alerts can be added here
  };
  
  module.exports = { sendAlert };
  